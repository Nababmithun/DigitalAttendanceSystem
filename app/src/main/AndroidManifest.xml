<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.codedleaf.sylveryte.attendanceapp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Attendance1.AdditionActivity"
            android:label="@string/add"
            android:parentActivityName=".Attendance1.MainActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Attendance1.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Attendance1.SummaryActivity"
            android:parentActivityName=".Attendance1.MainActivity" />
        <activity android:name=".Attendance1.About" />
        <activity android:name=".Attendance1.ParcentanceCal" />
        <activity android:name=".Attendance1.parcentanceeeeeeeeeee"></activity>
    </application>

</manifest>